{
  "name": "discussions",
  "private": true,
  "scripts": {
    "lint": "markdownlint-cli2",
    "lint:fix": "markdownlint-cli2 --fix",
    "test": "npm run lint"
  },
  "devDependencies": {
    "markdownlint-cli2": "^0.18.1"
  }
}
